type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  mesiboId: String
  age: String
  gender: String
  mesiboToken: String
  untitledfield: String
  name: String
}

type Match @model @auth(rules: [{allow: public}]) {
  id: ID!
  maleUser: ID!
  femaleUser: ID!
  isActive: Boolean!
  timeMatched: AWSTimestamp!
  dateLocation: String
  room: String
}
